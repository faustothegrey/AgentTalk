{
  "_comment": "O-2 launch config — see design/o2-plan.md. First rung where the worker WRITES and COMMITS. Absolute paths are required: startCommand.cwd resolves against the CLIENT root when relative.",

  "instance": {
    "orchestratorUrl": "http://127.0.0.1:3600",
    "recording": "runs/o2-investigation.ndjson",
    "readyTimeoutMs": 120000,
    "env": {
      "PORT": "3600"
    },
    "startCommand": {
      "command": "npm",
      "args": ["run", "backend"],
      "cwd": "/Users/fausto/Software/AgentTalk"
    }
  },

  "agents": [
    {
      "id": "op-worker-2",
      "provider": "claude",
      "model": "opus",
      "executionMode": "persistent",
      "workdir": "/private/tmp/att-op-2",
      "readyTimeoutMs": 120000
    }
  ],

  "goal": "Investigate backlog item BL-088 — the invariant harness answers the damage question but not the teardown question — and write a design document at design/bl088-investigation.md weighing its three recorded options and recommending one, with reasons. Commit that document to your branch; change no code.",

  "cap": {
    "wallClockMs": 1200000,
    "pollIntervalMs": 10000,
    "meter": {
      "url": "http://127.0.0.1:9899",
      "provider": "claude",
      "maxPercentDelta": 15
    }
  }
}
