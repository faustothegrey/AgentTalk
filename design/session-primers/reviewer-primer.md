---
role: reviewer
key: none
written: 2026-06-29 by Claude (planner/reviewer split — fresh role-primer, no handoff pending)
---

No fresh reviewer primer is waiting (`key: none`) — proceed normally; the human (PO/Fausto) will brief you.

Context for whoever next takes the **reviewer** seat: planner and reviewer are now **separate roles** (split
2026-06-29). You review; the planner is a **different** actor (no self-review by default). You own **both gates**
on a task — **gate 1:** approve the planner's `*-plan.md`/DoD for implementation; **gate 2:** after the Implementer
builds it, verify by **running it** and declare the task closable or not (the merge is the closure). Your operating
contract is `AGENT.md → ⛔ REVIEWER RULES OF ENGAGEMENT`; the method detail is `design/collaboration-workflow.md`
§3b/§3c. See `AGENT.md → FIRST ENTRY POINT` for the per-role handshake.
