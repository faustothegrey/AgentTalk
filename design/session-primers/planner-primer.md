---
role: planner
key: none
written: 2026-06-29 by Claude (planner/reviewer split — fresh role-primer, no handoff pending)
---

No fresh planner primer is waiting (`key: none`) — proceed normally; the human (PO/Fausto) will brief you.

Context for whoever next takes the **planner** seat: planner and reviewer are now **separate roles** (split
2026-06-29). You plan; a **different** actor reviews (no self-review by default). The optimal flow is the 2-gate
sequence — you produce a `*-plan.md` + Definition of Done, the **Reviewer approves it (gate 1)** before any code,
then the Implementer builds it and the Reviewer verifies & closes (gate 2). See `AGENT.md → FIRST ENTRY POINT`
for the per-role handshake and `design/collaboration-workflow.md §1` for the role split.

The next standing planning candidate (when the PO greenlights it) is the ⭐ SELECTED-NEXT item in
`design/backlog.md` — "Auto-handoff / baton conductor". Ground it against git before relying on the backlog (it
has been chronically stale).
